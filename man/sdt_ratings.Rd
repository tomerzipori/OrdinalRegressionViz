% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{sdt_ratings}
\alias{sdt_ratings}
\title{Simulated truth ratings of true and fake news headlines}
\format{
A data frame with 4,000 rows and 5 variables:
\describe{
\item{id}{Participant identifier (factor, 50 levels).}
\item{condition}{Between-participant condition: \code{control} or
\code{inoculation}.}
\item{time}{Measurement occasion: \code{pre} or \code{post} intervention.}
\item{target}{Type of headline: \code{fake} or \code{true}.}
\item{value}{Perceived-truth rating on an ordered 1--6 scale
(1 = "definitely fake", 6 = "definitely true").}
}
}
\usage{
sdt_ratings
}
\description{
A simulated trial-level dataset in a classic signal-detection design,
inspired by studies of psychological inoculation against misinformation:
participants in two conditions (control / inoculation) rated true and
fake news headlines before and after the intervention on a 6-point
perceived-truth scale.
}
\details{
The data were generated from an unequal-variance probit SDT model (fake
headlines centered at 0 with unit SD; true headlines shifted by d' with
SD 1.2), with a participant random intercept. In the simulation,
inoculation increases post-intervention discrimination (d') and makes the
response criteria stricter. See \code{data-raw/sdt_ratings.R} in the source
repository for the exact generating code.
}
\examples{
\dontshow{if (requireNamespace("ordinal", quietly = TRUE)) withAutoprint(\{ # examplesIf}
fit <- ordinal::clm(value ~ target * time, data = sdt_ratings, link = "probit")
summary(fit)
\dontshow{\}) # examplesIf}
}
\keyword{datasets}
