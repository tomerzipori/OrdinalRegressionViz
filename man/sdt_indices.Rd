% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sdt_indices.R
\name{sdt_indices}
\alias{sdt_indices}
\title{Signal detection indices of an ordinal regression model}
\usage{
sdt_indices(
  model,
  var_signal,
  var_group = NULL,
  var_facet = NULL,
  ci = 0.95,
  ndraws = NULL,
  ...
)
}
\arguments{
\item{model}{A cumulative regression model: \code{ordinal::clm()},
\code{ordinal::clmm()}, or a \code{brmsfit} from \code{\link[brms:brm]{brms::brm()}}.}

\item{var_signal}{Name (string) of the 2-level factor playing the role of
the SDT signal. Its first level is the noise distribution.}

\item{var_group, var_facet}{Optional names (strings) of additional 2-level
factor covariates; indices are computed for every combination of their
levels.}

\item{ci}{Width of the credible intervals (\code{brmsfit} only).}

\item{ndraws}{Optional number of posterior draws to use (\code{brmsfit} only);
\code{NULL} uses all draws.}

\item{...}{Passed on to methods.}
}
\value{
A data frame with one row per index and design cell: the levels
of \code{var_group}/\code{var_facet} (when supplied), \code{index}, \code{estimate},
\code{lower}, and \code{upper}.
}
\description{
Computes the signal detection theory (SDT) quantities implied by a
cumulative regression model, per design cell: the sensitivity index
(\code{d_prime}), the scale ratio of the signal and noise distributions
(\code{sigma_ratio}), the response thresholds (\verb{criterion ...}), and the area
under the implied ROC curve (\code{auc}).
}
\details{
Definitions follow the classic SDT conventions, on the latent scale of
the model's link function (normal for probit):
\itemize{
\item \code{d_prime} is the location difference between the signal (second level
of \code{var_signal}) and noise (first level) distributions, in units of the
noise distribution's scale.
\item \code{sigma_ratio} is the signal scale divided by the noise scale (1 unless
the model includes a \code{disc} part; \code{ordinal::clm()} scale models are not
supported).
\item \verb{criterion <k|k+1>} are the response thresholds of the cell, in raw
latent units (matching \code{\link[=SDT_distributions_plot]{SDT_distributions_plot()}}).
\item \code{auc} is the probability that a random signal draw exceeds a random
noise draw. It has a closed form under probit and is computed by
numeric integration for other links.
}

For \code{brmsfit} models the full posterior of every index is used, and the
returned \code{estimate} is the posterior median with a \code{ci} central credible
interval. For \code{clm}/\code{clmm} models the indices are point estimates
(\code{lower}/\code{upper} are \code{NA}); use the Bayesian interface if you need
uncertainty for the derived quantities.
}
\examples{
\dontshow{if (requireNamespace("ordinal", quietly = TRUE)) withAutoprint(\{ # examplesIf}
fit <- ordinal::clm(value ~ target * time, data = sdt_ratings, link = "probit")
sdt_indices(fit, var_signal = "target", var_group = "time")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=SDT_distributions_plot]{SDT_distributions_plot()}} and \code{\link[=bayesian_SDT_distribution_plot]{bayesian_SDT_distribution_plot()}}
for the corresponding latent-distribution displays.
}
